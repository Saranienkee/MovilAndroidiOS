import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyecto1Ionic',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
